buildscript {
    repositories {
        jcenter()
        maven { url = "http://files.minecraftforge.net/maven" }
    }
    dependencies {
        classpath 'net.minecraftforge.gradle:ForgeGradle:2.3-SNAPSHOT'
    }
}

apply plugin: 'net.minecraftforge.gradle.forge'
apply plugin: 'maven'

<<<<<<< HEAD
version = "1.4.0-1.12.2"
=======
version = "${mc_version}-${mod_version}"
>>>>>>> master
group = "com.ewyboy.bibliotheca"
archivesBaseName = "Bibliotheca"

sourceCompatibility = targetCompatibility = '1.8'
compileJava {
    sourceCompatibility = targetCompatibility = '1.8'
}

minecraft {
<<<<<<< HEAD
    version = "1.12.2-14.23.5.2847"
    runDir = "run"
    mappings = "snapshot_20171003"
=======
	version = "${mc_version}-${forge_version}"
	runDir = "run"
	mappings = "${mcp_mappings}"
	makeObfSourceJar = false
>>>>>>> master
}

repositories {  
    maven { url "http://tehnut.info/maven" }
}

dependencies {
<<<<<<< HEAD
    deobfCompile "mcp.mobius.waila:Hwyla:1.8.26-B41_1.12.2"
=======
    compile "mcp.mobius.waila:Hwyla:${waila_version}"
>>>>>>> master
}

processResources {
    inputs.property "version", project.version
    inputs.property "mcversion", project.minecraft.version

    from(sourceSets.main.resources.srcDirs) {
        include 'mcmod.info'
        expand 'version':project.version, 'mcversion':project.minecraft.version
    }
        
    from(sourceSets.main.resources.srcDirs) {
        exclude 'mcmod.info'
    }
}

jar {
    classifier = 'universal'
}

task deobfJar(type: Jar) {
    from sourceSets.main.output
    classifier = 'deobf'
}

task srcJar(type: Jar) {
    from sourceSets.main.allSource
    classifier = 'sources'
}

artifacts {
    archives deobfJar
    archives srcJar
}

uploadArchives {
    repositories {
        mavenDeployer {
            if (project.hasProperty("local_maven")) {
                repository(url: "file://${local_maven}")
                pom {
                    groupId = project.group
                    artifactId = project.archivesBaseName
                    project {
                        name project.archivesBaseName
                        packaging 'jar'
                        description 'Bibliotheca'
                        url 'https://github.com/EwyBoy/Bibliotheca'

                        scm {
                            url 'https://github.com/EwyBoy/Bibliotheca'
                            connection 'scm:git:git://github.com/EwyBoy/Bibliotheca.git'
                            developerConnection 'scm:git:git@github.com:EwyBoy/Bibliotheca.git'
                        }

                        issueManagement {
                            system 'github'
                            url 'https://github.com/EwyBoy/Bibliotheca/issues'
                        }

                        developers {
                            developer {
                                id 'EwyBoy'
                                name 'EwyBoy'
                                roles { role 'developer' }
                            }
                        }
                    }
                }
            }
        }
    }
}